# AGENTS.md

本文件适用于仓库根目录及全部子目录。除非更深层目录另有 `AGENTS.md`，所有自动化编码 Agent 都必须遵守本文约束。

## 1. 项目概览

本仓库包含三个协同模块：

- `src/main/java/com/movie/ticket/`：Java 17、Spring Boot 3.3.5 后端。
- `web/`：Vue 3、TypeScript、Vite、Element Plus 管理控制台。
- `extension/`：Chrome Manifest V3、TypeScript、Vite 浏览器扩展。

其他关键目录：

- `src/test/`：后端测试。
- `src/main/resources/db/migration/`：Flyway 数据库迁移。
- `src/main/resources/static/console/`：由 `web` 构建生成的控制台静态文件。
- `docs/`：接口、部署、联调和业务说明。
- `scripts/`：本地启动与测试脚本。

开始修改前，先阅读与任务直接相关的代码和文档；涉及核心业务流程时，同时阅读 `README.md`。不要根据文件名猜测现有行为。

## 2. 工作方式

- 先执行 `git status --short`，将已有改动视为用户工作，禁止覆盖、回退或顺手整理与任务无关的内容。
- 使用 `rg` / `rg --files` 定位代码，并先查找现有实现、DTO、测试和命名模式。
- 修改应聚焦任务本身，避免无关重构、全文件格式化或依赖升级。
- 未经明确要求，不执行 `git commit`、`git push`，不改写历史，不使用 `git reset --hard`、`git clean` 或其他可能丢失工作区内容的命令。
- 能从仓库确定的事项自行判断；只有需求会显著改变接口、数据模型、安全边界或业务规则时才向用户确认。
- 完成时说明修改内容、实际运行的验证命令及结果；未能运行的检查必须说明原因。

## 3. Fail Fast 与严格协议处理

开发业务代码必须遵循 **Fail Fast** 和 **Let It Crash** 原则。真实的业务错误、数据错误和接口异常必须显式暴露，禁止用防御性编程、默认值或 fallback 将错误伪装成正常结果。

### 协议处理顺序

业务代码必须严格遵守已经定义的业务规则、接口协议、数据结构和状态码，不得根据响应内容猜测结果，也不得在协议之外自行增加兼容逻辑。处理外部或内部接口响应时必须按以下顺序执行：

```text
校验响应结构
-> 读取协议明确规定的 status、code 或其他状态字段
-> 根据明确定义的状态码进入唯一对应分支
-> 校验该分支要求的数据结构
-> 状态码未知或数据不符合协议时立即失败
```

- 协议定义 `200` 为成功时，只执行成功分支。
- 协议定义的 `3xx` 业务状态必须严格按该状态的业务规则处理。
- `4xx`、`5xx` 或其他已定义状态码必须进入各自明确的异常分支。
- 未定义的状态码、缺失的状态字段、错误的字段类型或不符合约定的数据结构，必须抛出异常或显式返回协议错误。
- 必要的参数、权限、状态、业务规则和数据一致性校验必须保留。禁止的是校验失败后擅自猜测、修复、降级或继续执行。

### 禁止的容错与猜测

- 状态不满足成功条件时，禁止继续读取成功分支的 `data` 并执行业务。
- 禁止为缺失、类型错误或未知的状态字段设置默认状态码、默认成功结果或替代布尔值。
- 禁止通过 `data` 是否存在、数组是否为空或字段是否完整来反推请求成功与否。
- 禁止依次尝试未经协议定义的备用字段，例如 `data ?? result ?? payload ?? {}`。
- 响应格式不符合协议时，禁止返回空数组、空对象、默认值、缓存结果、旧数据、模拟数据或伪造成功响应。
- 禁止为了所谓兼容性，同时支持多套未经确认的响应格式。
- 禁止宽泛捕获异常后静默忽略、只打印日志并继续执行，或把所有异常转换成正常结果。
- 未经明确业务要求，禁止擅自添加自动重试、降级处理、跳过错误数据或备用业务流程。

### 异常处理边界

只有以下目的允许捕获异常：

- 补充必要且不含敏感信息的错误上下文后重新抛出。
- 转换为项目协议明确规定的业务异常，同时保留原始异常原因。
- 释放文件、连接、锁等资源；优先使用语言和框架提供的自动资源管理机制。
- 执行接口协议明确要求的异常处理。

除此之外，让异常继续向上抛出。全局异常处理器只负责将已知异常映射为协议响应，不得承载业务判断、吞掉未知异常或伪造成功结果。

如果需求、接口文档、状态码或数据结构存在缺失、矛盾或歧义，必须明确指出并停止实现受影响逻辑，不得自行假设接口行为或用 fallback 绕过问题。

### 代码组织

- 参考成熟电商项目的分层方式，保持职责单一、领域命名准确、状态流转显式、异常语义清楚。
- Controller 只负责接收和校验请求、调用业务服务并返回响应；业务规则放在 Service 或领域层。
- 禁止把复杂业务逻辑堆积在 Controller、工具类或全局异常处理器中。
- 正常路径应清晰直接；每条异常路径必须明确且可观察；未知状态必须立即失败。

## 4. 不可破坏的业务与安全约束

### 多用户数据隔离

- `/api/v1/app/**` 的数据访问必须绑定当前登录用户，查询、更新、删除均使用 `userId` 限定。
- 普通用户路径不得在用户范围查询失败后回退到全局查询，也不得接受客户端提交的 `userId` 作为授权依据。
- 管理员跨用户操作必须位于明确的管理接口和服务路径中，并保留权限校验与审计语义。
- 新增用户业务实体时，应延续 `UserScopedEntity`、`UserScopeContext` 和带 `userId` 的 Repository 查询模式，并增加隔离测试。
- 异步任务必须保存并恢复所属 `userId`；线程池、定时任务和异常路径结束后必须清理 `ThreadLocal`，防止租户上下文串线。

### 鉴权与凭据

- 不得提交真实账号、密码、Token、Cookie、OSS 密钥、数据库凭据或加密密钥。示例配置只使用环境变量和无效占位值。
- 不得记录原始会话 Token、插件/机器人 Token、密码、Cookie、支付凭证、完整图片 Base64 或上游敏感响应。
- Token 应只保存不可逆摘要；用户上游账号凭据必须通过现有 `CredentialCipher` 加密，禁止新增明文落库路径。
- 新接口必须明确属于公开、用户、管理员、插件或机器人中的哪一类，并同步检查 `SecurityConfig` 及对应拦截器。不要依赖前端路由守卫作为权限控制。
- 不得通过放宽 `permitAll`、关闭校验或硬编码万能 Token 来解决联调问题。

### 订单、支付与上游调用

- 创建订单、确认收款、任务入队和上游提交必须保持幂等；重试不能产生重复订单、重复扣款或重复发货。
- 外部网络调用不要长时间占用数据库事务或行锁。状态更新必须符合现有订单状态机，并覆盖失败、超时、重复回调和部分成功情形。
- 严格区分 `upstreamOrderNo`（上游 `orderNumber`）和 `upstreamOrderId`（取消接口使用的订单 ID），禁止互换。
- 金额使用 `BigDecimal` 和明确舍入规则，禁止以 `double` / `float` 承载业务金额。
- 不得在未核实上游结果时盲目重新提交订单。保留持久任务队列的唯一业务键、重试和锁定语义。
- 默认测试与本地验证优先使用 mock 上游；未经用户明确授权，不得调用真实下单、支付、取消、发货或生产环境接口。

## 5. 后端约束

- 保持现有分层：Controller 负责协议与校验，Service 承载业务和事务，Repository 负责持久化，Upstream Client 封装外部系统。
- 请求参数使用 DTO 和 Bean Validation；业务错误沿用 `BusinessException`，响应沿用 `ApiResponse` 统一结构，不在 Controller 中拼装另类响应格式。
- 依赖优先使用构造器注入。遵循相邻代码风格，不引入仅为缩短少量代码的新框架或抽象层。
- 修改 API、状态枚举或响应字段时，检查所有调用方：`web/`、`extension/`、相关 Controller/DTO、测试与 `docs/*.yaml` / Markdown 文档。
- 修复缺陷时添加能复现问题的测试；新增用户范围、鉴权、幂等、金额或状态流转逻辑时，必须增加相应测试。

## 6. 数据库约束

- 数据库结构变更只能新增下一个连续版本的 Flyway 文件 `V<next>__<description>.sql`。
- 已存在的迁移文件视为不可变，禁止修改、重命名、重排或复用版本号。
- 迁移需兼容 MySQL，明确索引、唯一约束、外键、空值和历史数据处理；大表高风险变更需在交付说明中指出上线影响与回滚方案。
- Java 实体、Repository 查询、迁移和相关测试必须同步修改。不要依赖 `ddl-auto` 代替生产迁移。
- 禁止在迁移中写入真实密钥，或无迁移策略地删除业务数据、缩窄字段、增加非空约束。

## 7. Web 控制台约束

- `web/src/` 是控制台源码；`src/main/resources/static/console/` 是构建产物，不要直接手改生成的 HTML、JS 或 CSS。
- API 调用统一经过 `web/src/api.ts`，保持 `ApiResponse` envelope、Bearer Token 和 401 清理逻辑。
- 管理员与普通用户工作区必须继续隔离；新增页面时同步考虑路由元数据、导航可见性和后端权限。
- 保持 TypeScript 类型完整，避免无必要的 `any`、非空断言和静默吞错。面向用户的错误信息应可理解，但不得泄露敏感响应。
- 控制台部署基路径为 `/console/`。修改控制台后执行构建，以同步后端静态资源。

## 8. 浏览器扩展约束

- 保持 Chrome Manifest V3 兼容；background、content script、page hook 的执行上下文不同，跨上下文通信必须验证消息来源与数据结构。
- Token 与 installation ID 沿用现有存储和请求头约定。日志与页面注入不得暴露凭据或完整业务敏感数据。
- 不随意扩大 `permissions`、`host_permissions`、`web_accessible_resources` 或 CSP；确有需要时说明原因和安全影响。
- 闲鱼页面 DOM 和内部协议可能变化。选择器、Hook 或消息解析变更应有失败保护，不能阻断宿主页面正常功能。
- 修改扩展源码后至少运行类型检查和构建，并确认 `manifest.json` 引用的产物名称仍然存在。

## 9. 配置、文档与生成物

- 本地私有配置使用未跟踪的 `application.yml` 或 `application-*.local.yml`；可提交的示例只修改 `application.example.yml` 等模板。
- 新增环境变量时同步更新示例配置和相关部署/本地环境文档。
- API 合同变化时更新对应 OpenAPI/ApiFox 文件及必要的 README 说明，避免文档与实现漂移。
- 不提交 `target/`、`node_modules/`、日志、IDE 文件或临时调试文件。
- 不直接编辑压缩/打包后的生成文件来实现功能；应修改源文件后通过构建生成。

## 10. 验证命令

按改动范围执行最小充分验证；跨模块契约变化需要验证所有受影响模块。

```bash
# 后端完整测试
mvn test

# 后端单个测试类
mvn -Dtest=OrderServiceTest test

# Web 控制台（在 web/ 中）
npm run build

# 浏览器扩展（在 extension/ 中）
npm run typecheck
npm run build
```

首次安装依赖或 lockfile 已变化时使用 `npm ci`；不要为了运行验证无故重写 lockfile。构建完成后再次检查 `git status --short` 和相关 diff，确认没有夹带临时文件、密钥或无关生成物。
